const ui = require("./ui");

test("ui init()", () => {
    ui.init();
});

test("ui setupAlertGroupUI()", () => {
    ui.setupAlertGroupUI();
});

test("ui setupGroupTooltips()", () => {
    ui.setupGroupTooltips();
});
